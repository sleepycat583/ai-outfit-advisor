# LangChain v1 / LangGraph native agent stack. Keep these versions aligned;
# langchain-chroma 1.1 requires the Chroma 1.x client and core 1.x APIs.
langchain==1.4.0
langchain-core==1.6.2
langchain-community==0.4.2
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2
langgraph==1.2.11
langgraph-checkpoint-postgres==3.1.2
psycopg[binary,pool]==3.3.5
streamlit==1.57.0
# Streamlit 的 gzip middleware 尚未兼容 Starlette 1.4 的 GZipResponder 接口。
starlette==1.3.1
dashscope==1.25.18
python-dotenv==1.0.0
requests==2.34.2
pillow==12.2.0
# Chroma 1.x supports NumPy 1.x; keep NumPy 2 out until the existing
# embedding/vector integrations are audited against it.
numpy==1.26.4
chromadb==1.5.9
# OpenTelemetry 1.44 and Chroma 1.x require protobuf >=5; keep a stable
# protobuf 5 release instead of the pre-v1 protobuf 3 pin.
protobuf==5.29.5
supabase==2.31.0
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-common==1.44.0
opentelemetry-exporter-otlp-proto-grpc==1.44.0
opentelemetry-proto==1.44.0
opentelemetry-semantic-conventions==0.65b0
